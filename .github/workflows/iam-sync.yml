name: IAM Sync

# Dispara quando repos.yml ou qualquer policy-template é alterado na main.
# É chamado também pelo workflow do repo creator após ele atualizar repos.yml.
on:
  push:
    branches:
      - main
    paths:
      - "iam/repos.yml"
      - "iam/policy-templates/**"
      - "iam/module_repo_iam/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  TF_VAR_github_org: ${{ github.repository_owner }}
  TF_VAR_oidc_provider_arn: ${{ secrets.AWS_OIDC_PROVIDER_ARN }}
  TF_VAR_aws_region: "us-east-1"

jobs:
  terraform:
    name: "Terraform Apply — IAM Roles"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_IAM_BOOTSTRAP_ROLE_ARN }}
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.7"

      - name: Terraform Init
        working-directory: iam
        run: terraform init

      - name: Terraform Validate
        working-directory: iam
        run: terraform validate

      - name: Terraform Plan
        working-directory: iam
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        working-directory: iam
        run: terraform apply -auto-approve tfplan
